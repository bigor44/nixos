# --- Nix Build Artifacts ---
result
result-*
repl-result

# --- Sécurité (CRITIQUE) ---
# Clés privées (ne jamais commiter)
keys.txt
*.key
.env

# --- Outils (Direnv) ---
.direnv/

# --- Éditeurs ---
*.swp
*.swo
*~
.idea/
.DS_Store

# sops-nix local age keys (never commit private keys)
*.age.txt
keys.txt
